<% content_for :title, "Изменение заказа" %>

<div class="w-full">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Изменение заказа</h1>
    <p class="text-sm text-gray-500 mt-1">Заказ #<%= @order.id %></p>
  </div>

  <%= render "form", order: @order %>
</div>
