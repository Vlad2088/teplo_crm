<% content_for :title, "Изменение клиента" %>

<div class="w-full">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Изменение клиента</h1>
    <p class="text-sm text-gray-500 mt-1"><%= @client.name %></p>
  </div>

  <%= render "form", client: @client %>
</div>
